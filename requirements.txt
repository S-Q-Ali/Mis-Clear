# Mis-Clear uses only the Python standard library — no external dependencies.
# tkinter (GUI), sqlite3 (browser DBs), urllib (blocklist), shutil/os (files)
# are all included with standard Python 3.x on Windows.
